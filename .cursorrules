# Creatalk
A full-stack video calling platform for creators and fans.

## Features
- Real-time video calls with Daily.co
- Secure payment processing with Stripe
- Advanced scheduling system
- Creator dashboard and analytics

## Tech Stack
- Next.js 15 (App Router) with Turbopack
- TypeScript (strict mode)
- Tailwind CSS + shadcn/ui (Zinc color scheme)
- ESLint + Prettier configuration
- Supabase (Auth, Database, Realtime, RLS)
- Daily.co (Video calling API with recording)
- Stripe (Test mode payments - one-time purchases)

## Development Strategy
### Supabase Development
- Supabase Cloudのみを使用し、ローカル環境は使用しない
- マイグレーションファイルはバージョン管理し、SQL Editorで実行
- RLSポリシーは全テーブルに適用
- Realtime機能を活用した状態同期

## Directory Structure
```
src/
├── app/                 # App Router pages
│   ├── (auth)/         # Auth route group
│   │   └── login/      # Login page
│   ├── dashboard/      # User dashboard
│   ├── creators/       # Creator listing & profiles
│   │   └── [id]/       # Creator detail pages
│   ├── creator/        # Creator management
│   │   ├── apply/      # Creator registration
│   │   ├── products/   # Product management
│   │   ├── schedule/   # Schedule management
│   │   └── earnings/   # Revenue dashboard
│   ├── call/           # Video call pages
│   │   └── [roomId]/   # Video call interface
│   ├── profile/        # Profile settings
│   └── api/            # API routes (minimal)
│       └── auth/       # OAuth callbacks only
├── components/         # React components
│   ├── ui/            # shadcn/ui components
│   ├── auth/          # Auth-related components
│   ├── call/          # Call-related components
│   ├── creator/       # Creator-specific components
│   └── layout/        # Layout components
├── actions/           # Server Actions
│   ├── auth.ts        # Authentication actions
│   ├── creator.ts     # Creator actions
│   ├── call.ts        # Call management actions
│   └── payment.ts     # Payment actions
├── lib/              # Utilities and configurations
│   ├── supabase.ts   # Supabase client
│   ├── daily.ts      # Daily.co client
│   ├── stripe.ts     # Stripe configuration
│   └── utils.ts      # General utilities
├── types/            # TypeScript type definitions
│   ├── database.ts   # Supabase database types
│   ├── auth.ts       # Auth types
│   └── call.ts       # Call-related types
└── hooks/            # Custom React hooks
├── useAuth.ts    # Authentication hook
└── useCall.ts    # Call management hook
```

## Code Style
- Use src/ directory structure
- Import alias: @/* → src/*
- Use Server Actions over API routes
- TypeScript strict mode
- ESLint + Prettier for code formatting
- Component names: PascalCase
- File names: kebab-case
- Always include proper error handling
- Use revalidatePath() for cache updates
- Use "use server" directive for server functions
- Form handling with Server Actions

## Business Logic
### Call Products (2 patterns)
1. Queue System: 21:00-22:00 time slots with breaks
2. Fixed Slots: 21:00-21:05, 21:08-21:13 specific timings

### Features Required
- Video/Audio ON/OFF control
- Screen sharing (for real-time illustration feedback)
- Chat (Creator can speak, Fan chat-only mode)
- Recording (Creator-configurable, 1-week access/download)
- Break system for queue management
- Payment before call (front-payment system)
- Real-time status updates

## Database Schema
- profiles (user info, linked to auth.users)
- creators (creator info, online status, ratings)
- call_products (type: queue/slot, pricing, recording settings)
- reservations (queue position, slot times, Daily.co room info)

## Page Structure
### Auth Pages
- /login - Email/Password + Google OAuth

### User Common Pages
- /dashboard - User-specific dashboard
- /profile - Profile settings

### Fan Pages
- /creators - Creator list with search/filter
- /creators/[id] - Creator profile & products
- /creators/[id]/products/[productId] - Purchase flow
- /call/[roomId] - Video call interface

### Creator Pages
- /creator/apply - Creator registration
- /creator/products - Product management (queue/slot types)
- /creator/schedule - Schedule management
- /creator/earnings - Revenue dashboard

## Daily.co Integration
- Room creation with recording enabled/disabled
- Participant limits (Creator + 1 Fan)
- Auto-expiry for fixed slots
- Recording URL storage and access control
- Break system for queue management

## Stripe Integration
- One-time payments for call purchases
- Test mode with test card numbers
- Webhook handling for payment confirmation

## UI Patterns
- Mobile-first responsive design
- Dark mode support
- shadcn/ui components as base
- Consistent loading states
- Error boundaries
- Real-time status updates (Supabase Realtime)
- Toast notifications for user feedback

## Development Environment
- Use Supabase Cloud (not local)
- Environment variables for API keys
- Git version control with meaningful commits
- ESLint rules for code quality

## Features Priority
1. Auth system & profiles
2. Creator listing & basic video calls
3. Payment integration & call products
4. Creator management tools & analytics

## API Strategy
- Daily.co: Video calls, recording, participant management
- Stripe: One-time payments for call purchases
- Supabase: All data operations via Server Actions
- API Routes only for: webhooks, OAuth callbacks

## Error Handling
- Try-catch blocks in Server Actions
- User-friendly error messages
- Loading states for async operations
- Form validation with proper feedback

## Testing Strategy
### Unit Testing
- Vitest for component and utility testing
- React Testing Library for component interactions
- Test coverage target: 80%+ for critical paths
- Mock Supabase and Daily.co in tests

### E2E Testing
- Playwright for end-to-end testing
- Critical user flows: auth, call booking, video calls
- Cross-browser testing (Chrome, Safari, Firefox)
- Mobile device testing

### Test Structure
```
src/
├── tests/          # Unit tests
├── components/
│   └── tests/      # Component tests
└── e2e/               # E2E tests
├── auth.spec.ts
├── booking.spec.ts
└── call.spec.ts
```

## Performance Goals
### Core Web Vitals
- LCP (Largest Contentful Paint): < 2.5s
- FID (First Input Delay): < 100ms
- CLS (Cumulative Layout Shift): < 0.1
- Lighthouse Performance Score: 90+

### Video Call Quality
- Connection establishment: < 3s
- Audio/Video latency: < 150ms
- Recording quality: 720p minimum
- Mobile compatibility: iOS Safari, Android Chrome

### Optimization Techniques
- Next.js Image optimization
- Code splitting with dynamic imports
- Supabase connection pooling
- Daily.co preconnection for faster calls

## Security Guidelines
### Authentication & Authorization
- Supabase RLS (Row Level Security) for all tables
- JWT token validation in middleware
- Session management with automatic refresh
- Role-based access control (Fan/Creator/Admin)

### API Security
- Environment variables for all API keys
- No sensitive data in client-side code
- Rate limiting for API endpoints
- Input validation and sanitization

### Data Protection
- HTTPS only in production
- Encrypted data storage (Supabase)
- PII handling compliance
- Video call data retention policies

### Security Checklist
- [ ] No API keys in git repository
- [ ] All database queries use RLS
- [ ] Input validation on all forms
- [ ] CORS properly configured
- [ ] Security headers implemented
- [ ] Regular dependency updates

## Code Quality Standards
### Code Review Criteria
- TypeScript strict mode compliance
- ESLint zero warnings
- Meaningful test coverage
- Documentation for complex functions
- Performance impact assessment

### Git Workflow
- Feature branches for all changes
- Meaningful commit messages
- PR reviews before merge
- Automated testing in CI/CD

## Monitoring & Analytics
### Error Tracking
- Sentry for error monitoring
- User session recordings for UX issues
- Performance monitoring

### Business Metrics
- User engagement tracking
- Call completion rates
- Revenue metrics
- Creator satisfaction scores

## UI Design Guidelines
### Design System Reference
- Inspiration: Vercel, Linear, Stripe, Discord UI patterns
- Modern, professional, developer-focused aesthetic
- Glass-morphism and subtle animations
- Micro-interactions for enhanced UX

### Color System
- Base: Zinc (shadcn/ui Zinc theme)
- Primary: Blue-500 (#3B82F6)
- Secondary: Zinc-500 (#71717A)
- Success: Green-500 (#10B981)
- Warning: Yellow-500 (#F59E0B)
- Error: Red-500 (#EF4444)
- Background: Dark theme preferred

### Typography Scale
- Core Strategy:
  - Use system fonts for body text (performance-focused)
  - Use BIZ UDPGothic (web font) for headings only
  - Web fonts are subset with minimal character sets

- System Font Stack (font-sans):
  - -apple-system
  - BlinkMacSystemFont
  - "Hiragino Kaku Gothic ProN"
  - "Hiragino Sans"
  - Meiryo
  - sans-serif

- Web Font (font-heading):
  - BIZ UDPGothic (weight: 700 only)
  - display: swap
  - preload: true
  - subset: latin

- Text Styles:
  - Headings: font-heading font-bold, (text-3xl, text-2xl, text-xl)
  - Body: font-sans font-normal
  - UI Elements: font-sans font-medium
  - Code: font-mono
  - Proper contrast ratios (4.5:1 minimum)

### Layout Principles
- 8px grid system for spacing
- Mobile-first responsive design
- Consistent component spacing
- Card-based information architecture
- Proper visual hierarchy

### Component Patterns
#### Navigation
- Sidebar navigation for dashboard
- Top navigation for marketing pages
- Breadcrumbs for deep navigation
- Mobile-friendly hamburger menus

#### Data Display
- Cards for grouped information
- Tables with sorting and filtering
- Charts for analytics (recharts)
- Progress indicators for loading
- Empty states with helpful messaging

#### Forms
- shadcn/ui form components
- Proper validation feedback
- Loading states during submission
- Error handling with toast notifications
- Accessibility compliance

#### Interactive Elements
- Hover effects with smooth transitions
- Click feedback (button states)
- Skeleton loading for async content
- Toast notifications for actions
- Modal dialogs for confirmations

### Animation Guidelines
- Subtle transitions (200-300ms)
- Easing functions: ease-in-out
- Hover effects on interactive elements
- Page transitions for route changes
- Loading animations for async operations

### Responsive Breakpoints
- Mobile: 320px - 768px
- Tablet: 768px - 1024px
- Desktop: 1024px+
- Test on multiple devices

### Accessibility Standards
- Semantic HTML elements
- ARIA labels where needed
- Keyboard navigation support
- Screen reader compatibility
- Color contrast compliance
- Focus indicators

### Component Library
#### Required shadcn/ui Components
- card, button, input, form
- avatar, badge, progress
- dialog, sheet, tabs
- skeleton, toast, dropdown-menu
- calendar, chart (for advanced features)

#### Custom Components
- VideoCallInterface (Daily.co integration)
- CreatorCard (creator listings)
- CallProductCard (service offerings)
- PaymentForm (Stripe integration)
- StatusIndicator (online/offline states)

### Page-Specific Design Patterns
#### Landing Page
- Hero section with gradient background
- Feature grid (3-column)
- Social proof sections
- Clear call-to-action buttons
- Footer with links

#### Dashboard
- Sidebar navigation
- Statistics cards grid
- Data tables with actions
- Quick action buttons
- Recent activity feed

#### Video Call Interface
- Full-screen video area
- Floating control buttons
- Collapsible chat sidebar
- Participant list
- Screen sharing controls

#### Creator Profile
- Header with avatar and stats
- Service offerings grid
- Reviews and ratings
- Booking calendar
- Portfolio showcase

### Implementation Notes
- Use Tailwind CSS for styling
- Implement dark mode by default
- Test on mobile devices frequently
- Optimize images with Next.js Image
- Ensure fast loading times (<2s LCP)

## Next.js 15 Specific Rules
### Async APIs
- cookies() is now async - always use await cookies()
- headers() is now async - always use await headers()
- searchParams in page props are now async

### Correct Usage Patterns
#### Server Components
```typescript
async function ServerComponent() {
  const cookieStore = await cookies()
  const headersList = await headers()
  // ...
}
```

#### Server Actions
```typescript
'use server'
async function serverAction() {
  const cookieStore = await cookies()
  // ...
}
```

#### Middleware
```typescript
import { cookies } from 'next/headers'

export async function middleware(request: NextRequest) {
  const cookieStore = await cookies()
  // ...
}
```

### Common Patterns to Avoid
- Using cookies() without await
- Using headers() without await
- Synchronous access to request data

### Authentication Patterns
### Server Components
- Use cookies() with await for auth checks
- createServerClient for server-side Supabase operations
- Redirect on auth failure
- SEO-friendly, fast initial load

### Client Components  
- AuthProvider for global auth state
- useAuth hook for component auth state
- Real-time auth state updates
- Interactive auth operations

### Hybrid Approach (Recommended)
- Server: Initial auth check + data fetching
- Client: Auth state management + interactions
- AuthProvider with server-provided initial state
- Server Actions for auth operations

### File Structure
```
lib/
├── auth.ts              # Server-side auth utilities
├── supabase-client.ts   # Client-side Supabase
└── supabase-server.ts   # Server-side Supabase
providers/
└── auth-provider.tsx    # Client auth context
middleware.ts            # Route protection
```

### Usage Patterns
- Protected pages: Use requireAuth() in Server Components
- Auth-dependent UI: Use useAuth() in Client Components  
- Auth operations: Server Actions with supabase-server
- Real-time updates: Client Components with auth provider

### Example Implementations

#### Server Component Auth Check
```typescript
// app/protected/page.tsx
import { requireAuth } from '@/lib/auth'

export default async function ProtectedPage() {
  await requireAuth()
  // Protected content...
}
```

#### Client Component with Auth
```typescript
// components/UserProfile.tsx
'use client'
import { useAuth } from '@/hooks/useAuth'

export function UserProfile() {
  const { user, loading } = useAuth()
  if (loading) return <div>Loading...</div>
  if (!user) return <div>Please login</div>
  return <div>Welcome {user.email}</div>
}
```

#### Server Action Auth
```typescript
// actions/auth.ts
'use server'
import { createServerClient } from '@/lib/supabase-server'

export async function serverAuthAction() {
  const cookieStore = await cookies()
  const supabase = createServerClient(cookieStore)
  // Auth operations...
}
```

#### Auth Provider Setup
```typescript
// providers/auth-provider.tsx
'use client'
import { createContext, useContext } from 'react'
import { useAuth } from '@/hooks/useAuth'

const AuthContext = createContext(null)

export function AuthProvider({ children, initialUser }) {
  const auth = useAuth(initialUser)
  return (
    <AuthContext.Provider value={auth}>
      {children}
    </AuthContext.Provider>
  )
}
```

### Common Patterns to Avoid
- Using cookies() without await
- Mixing client/server auth methods
- Duplicating auth logic across components
- Storing sensitive auth data in localStorage
- Not handling loading/error states

### Best Practices
- Always use TypeScript for auth-related code
- Implement proper error boundaries
- Use loading states during auth operations
- Keep auth logic centralized
- Regular security audits of auth code
- Proper error handling for auth failures